[package]
name = "sidestage-cli"
version.workspace = true
edition.workspace = true
license.workspace = true
authors.workspace = true
repository.workspace = true
description = "Host-side smoke tool for the SideStage shared mobile core."

[[bin]]
name = "sidestage"
path = "src/main.rs"

[dependencies]
sidestage-core = { path = "../sidestage-core" }
